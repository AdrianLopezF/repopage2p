{
  "name": "pagina2protection",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
  "dev": "concurrently \"npm run server\" \"npm run client\"",
  "server": "cd backend && npm run dev",
  "client": "cd frontend && npm run dev"
},
  "dependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
